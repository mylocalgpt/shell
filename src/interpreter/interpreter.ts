import type { AST } from '../parser/ast.js';

/**
 * Interpreter that executes an AST produced by the parser.
 * Stub - will be implemented in p2.
 */
export class Interpreter {
	/**
	 * Execute an AST and produce a result.
	 * Stub - throws "not implemented".
	 *
	 * @param _ast - The AST to execute
	 */
	execute(_ast: AST): never {
		throw new Error('not implemented: Interpreter.execute');
	}
}
