/**
 * Format a string using printf-style format specifiers.
 * Stub - will be implemented in p3.
 *
 * @param _format - The format string (e.g. "%s: %d")
 * @param _args - Arguments to substitute into the format string
 */
export function printf(_format: string, ..._args: string[]): string {
	throw new Error('not implemented: printf');
}
