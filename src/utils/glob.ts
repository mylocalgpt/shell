/**
 * Test whether a text string matches a glob pattern.
 * Stub - will be implemented in p2 or p3.
 *
 * @param _pattern - The glob pattern (e.g. "*.txt", "src/**\/*.ts")
 * @param _text - The text to test against the pattern
 */
export function globMatch(_pattern: string, _text: string): boolean {
	throw new Error('not implemented: globMatch');
}
